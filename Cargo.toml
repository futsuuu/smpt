[package]
name = "smpt"
version = "0.1.5"
edition = "2024"

[dependencies]
git2 = { version = "0.20", default-features = false }
clap = { version = "4.6", features = ["derive"] }

[profile.release]
lto = true
opt-level = 3
panic = "abort"
codegen-units = 1
